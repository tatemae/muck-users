こんにちは<%= h(@user.login) %>, 

要求が行われているパスワードをリセットします。 
場合は、この要求をしていない、単にこのメールを無視します。 
場合は、このリクエストをクリックするだけで、以下のリンクを作っていた： 

 <%= reset_password_url(@user.perishable_token) %> 

もし、URLにしようと動作しない上にコピーしてブラウザに貼り付けてください。 
場合、問題をしてください。持ち続けるお問い合わせお気軽に。